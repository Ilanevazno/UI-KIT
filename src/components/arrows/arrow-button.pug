mixin arrow-button(data = {})
  -
    const {
      direction = 'arrow-left',
      isDisabled = false,
    } = data;
  button(class="arrow-button arrow-button_" + direction disabled= isDisabled)
    div(class="arrow-button__inner")