mixin header-nav(data = {})
  -
   const {
     url = '',
     name = '',
   } = data;
  section.header-nav
    ul.header-nav__links
      each page in data
        li
          a.header-nav__links-item(href= page.url)= page.name