mixin stages(data = {})
  -
    const {
      maxStep = '',
      currentStep = '',
    } = data;
  div(class="widget-stages")
    ul.list-unstyled.multi-steps
      - for (var i = 0; i < maxStep; i += 1)
      - if ((i + 1) === currentStep)
        li.is-active
      - else
        li