mixin input-label (data = {})
  -
    const {
      name = '',
      labelType = '',
      placeholder = '',
    } = data;
  label
    input.input-label.input-label__item(type= labelType name= name placeholder= placeholder)
